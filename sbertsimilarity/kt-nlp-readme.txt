Setup:
create environment of:
1.python (>=3.6)
2.install sentence transformer model with pip : pip install -U sentence-transformers
3.PyTorch (at least 1.0.1)

Training model:
run finkt.py

Model evaluation:
run t1.py

Datasets:
Present in kt_datasets.

Model save path:
In output folder.

Parameters to be changed for execution:
1.train and dev dataset in finkt.py
2.corpus and queries in t1.py

challenges remaining:
1.building datasets.( providing two sentences with their human goldscore)
2.sentence trimming.(removal of verbage)



 
